# Contact form (server-only)
RESEND_API_KEY=

# Optional: increases GitHub API rate limits for the server-side portfolio sync.
# Use a token with read-only access to public repository metadata/content.
GITHUB_TOKEN=
